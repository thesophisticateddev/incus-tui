[pytest]
asyncio_mode = auto
testpaths = tests
filterwarnings =
    # The terminal widget execs immediately after pty.fork, so the multi-threaded
    # forkpty warning does not apply to us.
    ignore:This process .* is multi-threaded.*:DeprecationWarning
